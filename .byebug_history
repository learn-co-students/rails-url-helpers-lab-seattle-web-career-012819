exit
redirect_to student_path(@student)
@student.active
set_student
exit
student.to_s
student
@students
exit
if @student.active @student.active = false end 
if @student.active 
      @student.active = true
    else 
      @student.active = false
  if @student.active
    end
      @student.active = true
    else 
      @student.active = false
 if @student.active
@student.active
@student.active = 1
@student.active
@student
exit
@student
exit
link_to student.to_s, student_path(student)
link_to student.to_s, post_path(student)
student.to_s
student.first_name
student
Student.all
@students
exit
